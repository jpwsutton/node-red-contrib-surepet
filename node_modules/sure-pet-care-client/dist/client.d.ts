import { Pet, PetLocation } from './types';
export declare class SurePetCareClient {
    private token;
    private readonly baseUrl;
    authenticate: (email: string, password: string) => Promise<void>;
    private getState;
    getPets: () => Promise<Pet[]>;
    getPetByName: (name: string) => Promise<Pet>;
    getPetById: (id: number) => Promise<Pet>;
    setPetLocation: (id: number, location: PetLocation, since?: Date) => Promise<void>;
}
